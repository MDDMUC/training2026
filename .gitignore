node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Local data — back up the .db file by copying it, do not commit.
db/*.db
db/*.db-journal
db/*.db-wal
db/*.db-shm

# Vercel CLI link/output
.vercel

# Claude Code project-local settings
.claude/settings.local.json
